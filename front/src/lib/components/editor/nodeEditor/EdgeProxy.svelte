<!-- Phantom component for ic.node.Edge.
     NodeEditor reads props from childEntries; Svelte Flow renders the actual edge.
     This component exists only so the IC Factory can resolve the MATLAB type. -->
