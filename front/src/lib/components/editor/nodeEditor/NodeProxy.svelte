<!-- Phantom component for ic.node.Node.
     NodeEditor reads props from childEntries; Svelte Flow renders the actual node.
     This component exists only so the IC Factory can resolve the MATLAB type. -->
